#!/usr/bin/env python3

from loraSPI import loraSPI
from  sx127x import sx127x

bus: int = 0
dev: int = 0

spi: loraSPI = loraSPI(bus=bus, bus_dev=dev, cs_pin=8)
print(spi.dump())
spi.init()

dev: sx127x = sx127x(spi=spi, rst_pin=22)
dev.init()
dev.reset()
